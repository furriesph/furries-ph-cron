# Copilot instructions for `furries-ph-cron`

## Key reminder

- If this repo ever adds frontend code that injects markup with `innerHTML`, remember that scoped selectors will not style that runtime content. Use a static container-rooted global selector pattern so injected markup keeps its intended layout and typography.
